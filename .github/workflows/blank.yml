name: My First Automation

on:
  workflow_dispatch:
  push:
    branches: [main]
    inputs:
      customer:
        description: 'What is the customer ?'
        required: true
        type: string

jobs:
  build:
    runs-on: ubuntu-latest
    outputs:
      build_id: ${{ steps.build_step.outputs.build_id }}

    steps:
      - uses: actions/checkout@v3

      - name: build id
        id: build_id
        run: echo "build_id=$BUILD_ID" >> $GITHUB_OUTPUT

      - name: Run a multi-line script
        run: |
          echo this is the build id ${{ build_id.outputs.build_id }}
